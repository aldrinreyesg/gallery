  <div class="modal fade" id="cleanModal" tabindex="-1" role="dialog" aria-labelledby="cleanModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title">Eliminar Galería</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
          <form id="formDel" action="{{ path('admin_clean') }}" method="get">
            <p><b>¿Está seguro</b> de eliminar la galería ?</p>
            <div class="mt-2 float-right">
              <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancelar</button>
              <button type="submit" id="cleanSub" class="btn btn-primary">Eliminar</button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>