  <div class="modal fade" id="importModal" tabindex="-1" role="dialog" aria-labelledby="importModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title">Importar Galería</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
          <form id="formImp" action="{{ path('admin_import') }}" method="post">
            <p><b>¿Está seguro</b> de importar la galería ?</p>
            <p>Se utilizará la siguien url.</p>
            <div class="form-group">
              <label for="url">Url</label>
              <input type="text" class="form-control" id="url" name="url" value="{{ csv_url }}" placeholder="Url" required="true">
            </div>
            <div class="mt-2 float-right">
              <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancelar</button>
              <button type="submit" id="importSub" class="btn btn-primary">Importar</button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>